
Commands :
		# ipconfig /all --> display complete TCP/IP configuration for all network adapters
		# nslookup <site name> --> display the ip address of a site name
		# ipconfig /displaydns --> displays the cache DNS info
		# ipconfig /flushdns --> Delete the DNS cache
		# configure R1 to act as a DNS server
			R1(config)#ip dns server
			R1(config)#ip host R1 <Ip address> ---> configure host name address mapping
			R1(config)#ip name-server 8.8.8.8 ---> COnfigure a DNS server that R1 will query 
			R1(config)#ip domain lookup ---> Enable R1 to perfomr DNS queries
		# display hosts
			R1# show hosts


Things Covered :
		# The Purpose of DNS
		# Basic Function Of DNS
		# Configuring DNS in Cisco IOS

Purpose Of DNS :
		* DNS is used to resolve Human-readable names (google.com) to IP addresses

			- Machines like PCs and Servers doesnt use Names, they use IP addresses 

			- Names are easier for Humans to use and remember than IP addresses
		
			- When you type 'youtube.com' into a web browser, your device will ask a DNS 				  server for the IP address of youtube.com

			- The DNS server(s) your device uses can be manually configured or learned via 				  DHCP

		* Wireshark Capture

			- DNS 'A' record = Used to map names to IPv4 addresses

			- DNS 'AAAA' record = Used to map names to IPv6 addresses

			- Standard DNS queries/responses typically use UDP.
				--> Message greater than 512 bytes uses TCP
				--> Port 53 ise used

Basic Functions Of DNS :

		* DNS Cache 
			
			- Devices will save the DNS server's responses to a Local DNS cache
				---> As a result PC's dont have to query the server every single time they 
				want to access a particular destination

		* DNS Host File 
		
			- A local text file that maps hostnames to ip addresses before querying DNS 				  servers
				ex :
					windows : C:windows\System32\drivers\etc\hosts
					macOS/Linux : /etc/hosts
					
DNS in Cisco IOS :

		* For hosts in a network to use DNS, you dont need to configure DNS on the routers. They 		  will simply forward the DNS message like any other packets

			- However a Cisco router can be configured as a DNS server, although its rare
				# if an internal DNS server is used, usually its a windows or linux server

		* A cisco router can also be configured as a DNS client
			